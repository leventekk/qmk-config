# QMK Firmware for CRKBD

## Usage

If you would like to compile the config, just hit your commandline and use the following command:

```bash
$ make
```

This will compile the firmware and copy the file to the firmware folder.
